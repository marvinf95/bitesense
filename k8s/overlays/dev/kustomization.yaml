apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Dev overlay: tracks freshly-built images on the Pi (Makefile tags with short SHA).
images:
  - name: bitesense-backend
    newTag: dev
  - name: bitesense-frontend
    newTag: dev
