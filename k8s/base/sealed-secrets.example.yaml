# NOTE: Replace this file with a real SealedSecret generated by `kubeseal` in your
# cluster before applying. Plain Secret manifest below shows the schema only and
# MUST NOT be committed with real values.

apiVersion: v1
kind: Secret
metadata:
  name: bitesense-secrets
  namespace: bitesense
type: Opaque
stringData:
  BITESENSE_JWT_SECRET: replace-with-32-or-more-bytes-of-randomness
  GEMINI_API_KEY: replace-me
  ANTHROPIC_API_KEY: replace-me
