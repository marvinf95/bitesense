apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitesense-data
  namespace: bitesense
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
